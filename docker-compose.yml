version: '3.8'

services:
  backend:
    build: .
    ports:
      - "80:80"
    restart: always

  dashboard:
    image: nginx:alpine
    ports:
      - "8080:80"
    volumes:
      - ./threatmapper.html:/usr/share/nginx/html/index.html
    restart: always